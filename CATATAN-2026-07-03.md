# Catatan 2026-07-03 — SEO & Domain Migration

## Goal
- Fix sitemap tidak terbaca Google Search Console
- Migrasi domain dari pages.dev ke custom domain

## Progress

### Sitemap Issue
- Sitemap XML (856 URLs), XML renamed (sitemap-new.xml), dan TXT (964 URLs) semuanya "Tidak dapat membaca"
- Google bisa akses file (200 OK, `application/xml` / `text/plain`) tapi parser ekstrak 0 URL
- URL Inspection: homepage 200 OK, crawlable, indexable — tapi tidak diindeks (terakhir crawl 18 Juni)
- Root cause: BUKAN format sitemap — kemungkinan prioritas rendah Google untuk subdomain pages.dev

### Actions Taken
- Dibuat `sitemap.txt` (text sitemap, format paling sederhana) + `generate_text_sitemap()` di generator
- Update `robots.txt` include sitemap.txt
- Update workflow `update.yml`: loop ping 3 sitemap ke Google + Bing
- Commit `6015e9b`: add text sitemap + ping update
- Commit `a30e0e0` (sebelumnya): sitemap-new.xml + workflow ping

### Domain Migration: pages.dev → www.akankah.eu.org
- **Nameserver:** FreeDNS (afraid.org) → **Cloudflare** (`bruce.ns.cloudflare.com` / `opal.ns.cloudflare.com`)
- **DNS:** CNAME `www` → `trend-fashion-auto.pages.dev` (proxied)
- **Cloudflare Pages:** Custom domain `www.akankah.eu.org` — status **Verifying** (waiting for Active)

### Next Steps
- Tunggu custom domain Active → update SITE_URL di generator
- Deploy ulang dengan URL baru www.akankah.eu.org
- GSC: verify property + submit sitemap.txt

## Key Files Modified
- `generate_site.py` — add `generate_text_sitemap()`, output sitemap.txt, update robots.txt
- `.github/workflows/update.yml` — loop ping 3 sitemap
- `generated/sitemap.txt` — new text sitemap (964 URLs)
