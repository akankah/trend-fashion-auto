# Catatan 2026-07-03 — SEO & Domain Migration

## Goal
- Fix sitemap tidak terbaca Google Search Console
- Migrasi domain dari pages.dev ke custom domain

## Progress

### Sitemap Issue
- Sitemap XML (856 URLs), XML renamed (sitemap-new.xml), dan TXT (964 URLs) semuanya "Tidak dapat membaca"
- Google bisa akses file (200 OK, `application/xml` / `text/plain`) tapi parser ekstrak 0 URL
- URL Inspection: homepage 200 OK, crawlable, indexable — tapi tidak diindeks (terakhir crawl 18 Juni)
- **Domains tested:** pages.dev + www.akankah.eu.org — SAMA hasilnya (0 pages)
- **Sitemap formats tested:** XML, renamed XML, TXT — SEMUA gagal
- **Root cause:** BUKAN format/domain — kemungkinan **bug Cloudflare Pages + Google sitemap parser**

### Domain Migration: pages.dev → www.akankah.eu.org ✅
- **Nameserver:** FreeDNS (afraid.org) → **Cloudflare** (`bruce.ns.cloudflare.com` / `opal.ns.cloudflare.com`)
- **DNS:** CNAME `www` → `trend-fashion-auto.pages.dev` (proxied) ✅
- **Cloudflare Pages:** Custom domain `www.akankah.eu.org` — **Active** ✅
- **Response time:** 0.5-0.7s (dulu 4-7s via pages.dev)
- **GSC:** property sudah ada, tapi sitemap.txt tetap "Tidak dapat membaca" (0 pages)

### Actions Taken
- Dibuat `sitemap.txt` (text sitemap) + `generate_text_sitemap()` di generator
- `_headers` file added: Cache-Control, Content-Type explicit untuk sitemaps + X-Robots-Tag
- `sitemap-test.xml` minimal (3 URLs) — untuk isolasi apakah parser issue related to file size
- Generator SITE_URL → `www.akankah.eu.org`
- Workflow update: loop ping 3 sitemaps ke Google + Bing
- Robots.txt uses SITE_URL variable now (was hardcoded)

### Diagnosa
- Semua sitemap (3 format × 2 domain, termasuk 3-URL test) gagal dengan error "Tidak dapat membaca" — 0 pages
- Google Product Expert: ini **UI misleading** — artinya sitemap belum diproses Google, bukan error
- URL Inspection DNS masih gagal (error "Host tidak diketahui") — DNS baru belum propagate ke Google infrastructure
- Homepage bisa di-crawl (200 OK) tapi tidak diindeks — situs terlalu baru, perlu waktu

### Schema Implementation ✅
- **BreadcrumbList** — semua halaman (home, category, product, article, static)
- **Product** — tambah `brand`, `itemCondition: NewCondition` (sebelumnya hanya name+price+rating)
- **CollectionPage** — category + articles index
- **Article** — tambah `mainEntityOfPage`, `url` di publisher/author
- **WebPage** — tambah `url`, `url` di publisher

### Bing Webmaster Tools ✅
- Sitemap: `https://www.akankah.eu.org/sitemap.txt`
- **Status: Successfully processed** ✅
- **URLs discovered: 972** ✅
- Bukti bahwa sitemap valid — GSC "Cannot read" adalah misleading UI

### Next Steps
- DNS propagate ke Google (24-48h) — setelah itu URL Inspection bisa digunakan
- Request Indexing homepage di GSC setelah DNS propagate
- Backlink & social signal untuk percepat discovery Google
- Nanti tambah GA4 kalau traffic mulai masuk

## Key Files Modified
- `generate_site.py` — SITE_URL baru, _headers, sitemap-test.xml, generate_text_sitemap()
- `.github/workflows/update.yml` — semua URL migrasi, ping include sitemap-test.xml
- `templates/*.html` — BreadcrumbList + schema improvements (6 templates)
- `CATATAN-2026-07-03.md` — ini

## Commits
- `a30e0e0` sitemap-new.xml + workflow ping
- `6015e9b` add text sitemap + ping update
- `8e5a105` docs: daily notes
- `aad86ce` migrate SITE_URL → www.akankah.eu.org
- `9ee41c3` add _headers + sitemap-test.xml
- `80cc05e` add sitemap-test.xml to ping list
- `6ec8820` fix relative paths in _headers
- `87076ce` feat: BreadcrumbList + CollectionPage schema, improve Product/Article
- `f1cad9d` docs: update catatan with schema + diagnosis
